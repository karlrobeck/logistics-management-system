{
  "extends": "@getcronit/pylon/tsconfig.pylon.json",
  "compilerOptions": {
    // add Bun type definitions
    "types": ["bun-types"],
    // specify output directory to avoid overwriting input files
    "outDir": "dist"
  },
  "include": ["pylon.d.ts", "src/**/*.ts", "gqty/**/*.ts"]
}
